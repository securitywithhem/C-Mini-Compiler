/*
 * Simple valid C program — demonstrates basic features:
 * function declarations, parameters, local variables, arithmetic, return.
 */
int add(int a, int b) {
    return a + b;
}

int main() {
    int x = 5;
    int y = 10;
    int sum = add(x, y);
    return sum;
}
